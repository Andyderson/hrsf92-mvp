import React from 'react';
import ReactDOM from 'react-dom';
import axios from 'axios';

class App extends React.Cfomponent {
    constructor(props) {
        super(props);
        this.state = {

        };
    };

    render () {
        return (
            <div>React Testing</div> 
        );
    }    
};

export default App;